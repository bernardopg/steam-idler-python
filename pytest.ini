[pytest]
pythonpath = src
# Kill hung tests deterministically (gevent/steam mocks can deadlock).
# thread method: signal-based timeouts don't fire inside gevent greenlets.
timeout = 120
timeout_method = thread
filterwarnings =
    ignore:datetime.datetime.utcfromtimestamp\(\) is deprecated:DeprecationWarning:google.protobuf.*
